# HPO study config for MomentumGatekeeper.
#
# Pair with config/strategies/momentum_gatekeeper.yaml:
#   python -m scripts.experiment tune \
#       --config config/strategies/momentum_gatekeeper.yaml \
#       --hpo-config config/hpo/momentum_gatekeeper.yaml

study_name: spy_momentum_gatekeeper_sharpe
n_trials: 100
n_jobs: 2
sampler: tpe
pruner: median
seed: 42
