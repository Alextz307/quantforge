data:
  source: yfinance
  tickers: [SPY]
  start: 2007-01-01
  end: 2010-12-31
  interval: daily
validation:
  holdout_pct: 0.0
  # 6-month test windows: matches spy_daily_covid for cross-stress-window
  # comparability. The 2008 universe IS the eval set (holdout_pct=0.0),
  # so all 1006 bars feed the three walk-forward folds.
  test_size: 126
