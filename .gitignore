# ── Python ──
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
dist/
build/
*.so
*.pyd

# ── Virtual environments ──
.venv/
venv/
env/

# ── C++ build artifacts ──
cpp/build/
*.o
*.a
*.dylib
*.dll
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
compile_commands.json
Makefile.cmake

# ── IDE ──
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
cpp/.cache/

# ── Testing / Linting ──
.mypy_cache/
.pytest_cache/
.ruff_cache/
htmlcov/
.coverage
*.gcda
*.gcno

# ── Experiment results (ephemeral) ──
# All experiment-results outputs are ephemeral and untracked.
experiment_results/

# ── Secrets / local config ──
.env
*.local

# ── scikit-build-core ──
_skbuild/

# ── Profiling ──
*.profdata
*.profraw

# ── Data cache ──
*.parquet
data_cache/
# Committed offline fixtures (used by offline tests).
!tests/fixtures/*.parquet

# ── Webapp ──
webapp/data/
webapp/.env
webapp/frontend/node_modules/
webapp/frontend/dist/
webapp/frontend/coverage/
webapp/frontend/src/api/generated/
